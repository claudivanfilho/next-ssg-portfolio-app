const Footer = () => {
  // TODO fix intl
  // const { formatMessage } = useIntl();

  return (
    <footer className="fixed bottom-0 w-full py-2 text-sm font-bold text-center text-white bg-purple-700">
      Created By Claudivan
      {/* {formatMessage({ id: "created-by" })} */}
    </footer>
  );
};

export default Footer;
